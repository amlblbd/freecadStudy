
好的，我会用中文继续分析。根据之前的对话和搜索结果，我已经找到了FreeCAD Application类中关于`_pendingDocs`队列和`_docReloadAttempts`映射的实现细节。

## 核心发现总结

### 1. 文档加载队列机制

在`Application.cpp`的第700-900行中，我找到了`openDocuments()`方法的完整实现，这是FreeCAD处理多文档加载的核心方法：

**主要特性：**
- 使用`_pendingDocs`队列存储待处理的文档路径
- 采用多轮循环处理，直到队列为空
- 支持部分加载模式，提高大项目启动性能
- 实现文档依赖关系排序

### 2. 重试和重新加载机制

**关键实现细节：**

1. **部分文档重新加载**：当部分加载的文档因对象被访问需要完整加载时，会被重新添加到`_pendingDocs`队列

2. **延迟重新打开机制**：文档被添加到`_pendingDocsReopen`队列而非立即重新打开，避免冲突

3. **循环依赖防护**：使用`_docReloadAttempts`映射跟踪重载尝试次数，防止无限递归

### 3. 私有文档打开方法

`openDocumentPrivate()`方法（第900-1100行）处理单个文档的详细逻辑：
- 检查文档是否存在
- 处理部分加载场景
- 管理`_pendingDocsReopen`队列
- 处理文档恢复和异常

### 4. 异常处理和错误恢复

系统包含完善的异常处理：
- 区分主文档和次要文档的错误处理策略
- 文件不存在错误的特殊处理
- 未知异常的捕获和文档清理

### 5. 架构优势

这个文档加载系统展现了以下优势：
- **鲁棒性**：处理复杂的文档依赖和部分加载场景
- **性能**：支持部分加载以提升大项目启动速度
- **稳定性**：防止无限递归，处理各种错误情况
- **灵活性**：支持完整和部分文档加载模式

您提供的`signalBeforeChangeObject`信号定义（第284行）是这个系统的一部分，用于在文档对象属性更改前发出通知，确保UI和其他组件能够正确响应文档状态变化。

这个架构设计很好地处理了CAD软件中常见的复杂文档依赖关系和大型项目加载性能问题。
        