[requires]
# Conan 依赖声明：
# - qt/6.6.2：通过 Conan 获取 Qt6，并由 CMakeDeps/CMakeToolchain 自动生成 CMake 可用的包配置与 toolchain
qt/6.6.2
# - zlib/1.3.1：显式固定 zlib 版本，避免某些依赖链（例如 libxml2）对 zlib 版本范围要求导致的冲突
zlib/1.3.1

[generators]
# 生成器说明：
# - CMakeDeps：为每个 Conan 包生成 <Pkg>Config.cmake / Targets.cmake，让 CMake 能 find_package(...)
# - CMakeToolchain：生成 conan_toolchain.cmake 与 CMakePresets.json（如 conan-release）
CMakeDeps
CMakeToolchain
